name: default
on:
  pull_request_target:
    types: labeled
jobs:
  build:
    permissions:
      actions: write
    runs-on: ubuntu-latest
    steps:
      - name: dump
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: |
          echo "test 333"
          echo "$GITHUB_CONTEXT"
